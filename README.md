Nikon AF Fine-Tune Report Tool v14

Fixes applied:
1. Reference number now auto-generates on the home screen as NIK-YYYYMMDD-001.
2. Saved lens number now updates the Fine-tune menu:
   - No number assigned
   - Saved lens No. 1 to 20
3. Single focal length mode now marks the Fine-tune menu as AF mode rather than leaving the menu visually unchanged.
4. PDF report uses the same clean Nikon menu image and mirrors the lens number/single mode display.
5. Old reference menu images are removed; only nikon-menu-clean.png is used.

Upload all files and delete old nikon-app-v*.js and nikon-menu-reference*.png from GitHub.

Nikon AF Fine-Tune Report Tool v10

Tested clean correction:
- Uses a new blank-lens-panel Nikon menu image.
- The old fixed 70-200mm label is removed from the image itself.
- The live lens label is now text-only, smaller and positioned inside the original lens info panel.
- No large overlay box is used.
- Descenders such as the 'g' in Sigma are no longer clipped.
- Saved lens dropdown retains 'No number assigned' and Lens No. 1-20.
- PDF uses the same blank image and small text-only lens label.
- PDF menu is contained as a full fixed-aspect image so the lower half is not cut off.

Upload all files to GitHub and remove old nikon-app-v8.js / nikon-app-v9.js.

Nikon AF Fine-Tune Report Tool v21

This version replaces the broken v20 JavaScript with a syntax-checked clean version.

Fixes:
- Auto reference number writes to jobRef on page load.
- Lens description updates the Fine-tune menu live.
- Saved lens number dropdown is populated and updates the menu live.
- Subject distance dropdowns are populated up to 24 m.
- Single Fine-Tune mode swaps to nikon-menu-clean-single.png.
- Wide/Tele mode swaps back to nikon-menu-clean.png.
- PDF report uses the matching menu image and current form values.

Important GitHub upload:
Delete old nikon-app-v*.js files before uploading this package.

Nikon AF Fine-Tune Report Tool v12

Correction:
- Restores the original Nikon Fine-tune menu image proportions.
- Camera and mountain symbols are retained.
- Removes the HTML replacement menu.
- Uses a blanked lens-panel image so the old 70-200 text cannot show through.
- Adds a small text-only live lens label inside the lens panel.
- Fixes the stray 'A' before the lens name.
- PDF report uses the same full image menu with object-fit contain, so the full menu should be visible.
- 'No number assigned' remains available.

Upload all files to GitHub and remove old nikon-app-v10.js / nikon-app-v11.js if present.

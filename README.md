Nikon AF Fine-Tune Report Tool v11

This build removes the image/overlay approach completely.

- The Nikon menu is now rendered as real HTML/CSS.
- No old 70-200mm label can remain.
- No lens-label overlay box is used.
- Lens description and saved lens number are rendered directly into the menu.
- PDF uses the same menu component, so it should not crop the menu image.
- 'No number assigned' remains available.

Upload all files and remove old nikon-app-v*.js and nikon-menu-reference*.png files from GitHub.

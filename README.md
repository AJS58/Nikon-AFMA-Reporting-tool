Nikon AF Fine-Tune Report Tool v4

Update:
- Uses the approved generated Nikon menu image as the actual visual base.
- Removes the CSS-built inner scale overlay that caused overlap.
- Keeps live lens description and saved lens number overlay.
- Keeps the working controls and PDF/report generation.
- Uses nikon-app-v4.js to avoid browser cache issues.

Upload all unzipped files to GitHub root and commit changes.

Nikon AF Fine-Tune Report Tool v5

Hard reset visual fix:
- Uses the approved Nikon menu image directly.
- Removes the CSS-generated inner WIDE/TELE menu that was causing distortion and overlap.
- Keeps live controls, result cards, scales, LP/mm fields and report generation.
- PDF report uses the same approved Nikon image.
- Uses nikon-app-v5.js to avoid browser cache issues.

Upload all unzipped files to GitHub root and commit changes.
